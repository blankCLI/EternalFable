# Claude / Fable Adapter Notes

EternalFable presents a public "Claude Fable" adapter profile for demo output.
That label is intentionally product-style and redacted. It is not an official
Anthropic model name.

Live calls use `ANTHROPIC_MODEL`; the default offline path is deterministic and
safe for screenshots, READMEs, and GitHub demos.
